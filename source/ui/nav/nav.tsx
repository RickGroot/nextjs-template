import React from 'react';
import $ from './nav.module.scss';

const Nav = () => {
    return (
        <nav className={$.container}>
            <p>Navigation module</p>
        </nav>
    );
};

export default Nav;
